/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // swcMinify: true, // Uncomment if you want to use SWC minification, removed for production compatibility
}

module.exports = nextConfig